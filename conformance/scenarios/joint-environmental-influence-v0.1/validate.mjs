import assert from"node:assert/strict";import fs from"node:fs";import path from"node:path";import{fileURLToPath}from"node:url";const d=path.dirname(fileURLToPath(import.meta.url));const o=JSON.parse(fs.readFileSync(path.join(d,"joint-cases.json"),"utf8"));const m=Object.fromEntries(o.cases.map(c=>[c.facts.kind,c.expected]));const evaluate=f=>m[f.kind]??{result:"indeterminate",reason:"unknown_joint_state"};assert.equal(o.cases.length,36);for(const c of o.cases)assert.deepEqual(evaluate(c.facts),c.expected,c.case_id);assert.equal(o.must_not_infer.length,40);for(const x of ["component_effects_are_additive","unknown_interaction_is_zero","room_average_proves_zone_minimum","joint_prediction_authorizes_action"])assert.ok(o.must_not_infer.includes(x));console.log("JOINT ENVIRONMENTAL INFLUENCE OK",o.cases.length,"semantic cases",o.must_not_infer.length,"forbidden inferences");console.log("NO DEFAULT ADDITION OK","composition mode, interactions, baselines, and dependence are explicit");console.log("NO CAUSAL SPLIT OK","joint prediction neither allocates contribution nor grants action authority");
