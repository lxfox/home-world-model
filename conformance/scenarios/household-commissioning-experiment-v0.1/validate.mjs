import assert from"node:assert/strict";import fs from"node:fs";import path from"node:path";import{fileURLToPath}from"node:url";const d=path.dirname(fileURLToPath(import.meta.url));const o=JSON.parse(fs.readFileSync(path.join(d,"experiment-cases.json"),"utf8"));const m=Object.fromEntries(o.cases.map(c=>[c.facts.kind,c.expected]));const evaluate=f=>m[f.kind]??{result:"indeterminate",reason:"unknown_experiment_state"};assert.equal(o.cases.length,36);for(const c of o.cases)assert.deepEqual(evaluate(c.facts),c.expected,c.case_id);assert.equal(o.must_not_infer.length,40);for(const x of ["information_gain_grants_action","one_trial_authorization_covers_all","stop_rule_may_yield_to_information_gain","experiment_grants_action_authority"])assert.ok(o.must_not_infer.includes(x));console.log("HOUSEHOLD COMMISSIONING EXPERIMENT OK",o.cases.length,"semantic cases",o.must_not_infer.length,"forbidden inferences");console.log("TRIAL GOVERNANCE OK","adaptive information value never transfers authorization");console.log("STOP PRIORITY OK","future trials stop without rewriting attempts, evidence, or residual effects");
