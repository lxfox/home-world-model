# Reusable Value Rule v0.1 oracle

This oracle tests the [Reusable Value Rule Profile](../../../spec/profiles/reusable-value-rule/v0.1/README.md) with 36 semantic cases and 40 forbidden inferences. It covers identity, revision activation, state, exact-use applicability, expiry/review, exceptions, conflict, drift, question suppression, and downstream boundaries.

Run `node conformance/scenarios/reusable-value-rule-v0.1/validate.mjs`.
