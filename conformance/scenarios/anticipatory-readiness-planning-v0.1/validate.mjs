import assert from"node:assert/strict";import fs from"node:fs";import path from"node:path";import{fileURLToPath}from"node:url";const d=path.dirname(fileURLToPath(import.meta.url));const o=JSON.parse(fs.readFileSync(path.join(d,"readiness-cases.json"),"utf8"));const m=Object.fromEntries(o.cases.map(c=>[c.facts.kind,c.expected]));const evaluate=f=>m[f.kind]??{result:"indeterminate",reason:"unknown_readiness_state"};assert.equal(o.cases.length,36);for(const c of o.cases)assert.deepEqual(evaluate(c.facts),c.expected,c.case_id);assert.equal(o.must_not_infer.length,40);for(const x of ["forecast_is_future_fact","demand_time_is_dispatch_time","cancellation_is_rollback","preparation_plan_authorizes_action"])assert.ok(o.must_not_infer.includes(x));console.log("ANTICIPATORY READINESS PLANNING OK",o.cases.length,"semantic cases",o.must_not_infer.length,"forbidden inferences");console.log("THREE TIMELINES OK","demand, response, and hold/decay windows remain distinct");console.log("NO FORECAST AUTHORITY OK","eligible plan candidates still require Task, Proposal, Authorization, and Attempt gates");
