import assert from"node:assert/strict";import fs from"node:fs";import path from"node:path";import{fileURLToPath}from"node:url";const d=path.dirname(fileURLToPath(import.meta.url));const o=JSON.parse(fs.readFileSync(path.join(d,"procurement-cases.json"),"utf8"));const m=Object.fromEntries(o.cases.map(c=>[c.facts.kind,c.expected]));const evaluate=f=>m[f.kind]??{result:"indeterminate",reason:"unknown_procurement_state"};assert.equal(o.cases.length,36);for(const c of o.cases)assert.deepEqual(evaluate(c.facts),c.expected,c.case_id);assert.equal(o.must_not_infer.length,40);for(const x of ["product_model_is_offer","in_stock_is_reserved","shopping_agent_session_transfers","procurement_qualification_authorizes_purchase"])assert.ok(o.must_not_infer.includes(x));console.log("PROCUREMENT OFFER QUALIFICATION OK",o.cases.length,"semantic cases",o.must_not_infer.length,"forbidden inferences");console.log("COMMERCIAL IDENTITY OK","model, variant, listing, offer, order, asset, and endpoint remain distinct");console.log("NO PURCHASE AUTHORITY OK","shortlist qualification transfers no cart, payment, fulfilment, or installation power");
