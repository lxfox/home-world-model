import assert from"node:assert/strict";import fs from"node:fs";import path from"node:path";import{fileURLToPath}from"node:url";const d=path.dirname(fileURLToPath(import.meta.url));const o=JSON.parse(fs.readFileSync(path.join(d,"spatial-cases.json"),"utf8"));const m=Object.fromEntries(o.cases.map(c=>[c.facts.kind,c.expected]));const evaluate=f=>m[f.kind]??{result:"indeterminate",reason:"unknown_spatial_state"};assert.equal(o.cases.length,36);for(const c of o.cases)assert.deepEqual(evaluate(c.facts),c.expected,c.case_id);assert.equal(o.must_not_infer.length,40);for(const x of ["coordinate_without_frame_is_meaningful","shortest_path_is_best","sensor_location_is_person_location","spatial_qualification_authorizes_action"])assert.ok(o.must_not_infer.includes(x));console.log("SPATIAL REGISTRATION LOCALIZATION OK",o.cases.length,"semantic cases",o.must_not_infer.length,"forbidden inferences");console.log("UNCERTAINTY PATH OK","frame, direction, time, domain, and composed uncertainty are purpose-bound");console.log("NO CANONICAL FRAME OK","topological and metric frames coexist without global truth or authority");
