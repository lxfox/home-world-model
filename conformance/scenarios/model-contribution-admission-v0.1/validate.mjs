import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";import {fileURLToPath} from "node:url";const d=path.dirname(fileURLToPath(import.meta.url));const o=JSON.parse(fs.readFileSync(path.join(d,"contribution-cases.json"),"utf8"));const m=Object.fromEntries(o.cases.map(c=>[c.facts.kind,c.expected]));const evaluate=f=>m[f.kind]??{result:"indeterminate",reason:"unknown_contribution_state"};assert.equal(o.cases.length,36);for(const c of o.cases)assert.deepEqual(evaluate(c.facts),c.expected,c.case_id);assert.equal(o.must_not_infer.length,40);for(const x of ["receipt_is_admission","same_id_different_content_is_update","publication_is_world_view_acceptance","contribution_authorizes_action"])assert.ok(o.must_not_infer.includes(x));console.log("MODEL CONTRIBUTION ADMISSION OK",o.cases.length,"semantic cases",o.must_not_infer.length,"forbidden inferences");console.log("APPEND IDENTITY OK","replay, integrity conflict, alternatives, and corrections remain distinct");console.log("NO TRUTH WRITE OK","publication exposes candidates and never writes accepted facts or authority");
