import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";import {fileURLToPath} from "node:url";
const dir=path.dirname(fileURLToPath(import.meta.url));const oracle=JSON.parse(fs.readFileSync(path.join(dir,"model-cases.json"),"utf8"));
const base={basis:"composite",use:"comparison",claim:"prediction",comparability:"complete",domain:"inside",units:"compatible",uncertainty:"budget",authorization:"allowed",standing:"accepted",disagreement:"none",currency:"current",source:"bound",validation:"independent",target:"planned"};
const a=(result,reason)=>({result,reason});
const evaluate=o=>{const f={...base,...o};
 if(f.target==="commissioned")return a("historical_only","target_empirical_model_required_for_current_use");
 if(f.currency==="stale")return a("indeterminate","target_design_binding_stale");if(f.currency!=="current")return a("indeterminate","target_currency_indeterminate");
 if(f.source!=="bound")return a("indeterminate","exact_source_binding_missing");
 if(f.authorization==="denied")return a("not_qualified","source_use_not_authorized");if(f.authorization!=="allowed")return a("indeterminate","source_authorization_indeterminate");
 if(f.standing==="denied")return a("not_qualified","source_evidence_not_admitted");if(f.standing!=="accepted")return a("indeterminate","source_evidence_standing_indeterminate");
 if(f.claim==="observation"&&f.basis==="simulation")return a("not_qualified","simulation_is_not_observation");
 if(f.comparability==="failed")return a("not_qualified","source_target_not_comparable");if(f.comparability!=="complete")return a("indeterminate","required_comparability_missing");
 if(f.domain==="outside")return a("not_qualified","target_outside_source_domain");
 if(f.units!=="compatible")return a("indeterminate","unit_conversion_not_established");
 if(f.uncertainty!=="budget")return a("indeterminate","uncertainty_budget_inadequate");
 if(f.disagreement==="unresolved")return a("indeterminate","source_disagreement_unresolved");
 if(f.validation==="training_only")return a("indeterminate","model_fit_not_target_validation");
 if(f.use==="acceptance")return a("not_qualified","planning_evidence_cannot_accept_installation");if(f.use==="compliance")return a("not_qualified","planning_evidence_cannot_establish_compliance");if(f.use==="control")return a("not_qualified","planning_evidence_cannot_authorize_control");if(f.use==="purchase")return a("not_qualified","prediction_does_not_create_transaction");if(f.use==="dispatch")return a("not_qualified","prediction_does_not_authorize_action");
 if(f.basis==="manufacturer"&&f.use==="catalog")return a("qualified_with_limits","declaration_supports_declared_property_only");if(f.basis==="analogue")return a("qualified_with_limits","bounded_analogue_transfer");if(f.basis==="simulation"||f.use==="comparison")return a("qualified_with_limits","model_conditioned_planning_use");return a("qualified_with_limits","planning_use_with_declared_limits")};
assert.equal(oracle.cases.length,36);for(const c of oracle.cases)assert.deepEqual(evaluate(c.facts),c.expected,c.case_id);assert.equal(oracle.must_not_infer.length,40);
console.log("PLANNING PREDICTION QUALIFICATION OK",oracle.cases.length,"semantic cases",oracle.must_not_infer.length,"forbidden inferences");
console.log("TRANSFER BOUNDARY OK","declaration, simulation, analogue, and target observation remain distinct");
console.log("DECISION BOUNDARY OK","planning qualification does not create acceptance, compliance, purchase, or control authority");
